<div class="row">
  <div class="col-md-4">
    <p>Guidelines for summaries</p>
      <ul>
        <li>Make sure it doesn't ryhmes.</li>
        <li>Don't use the letter "x y or z".</li>
        <li>Make sure to summarize it i.e keep it short.</li>
      </ul>
    </div>
  <div class="col-md-8">
    <%= form_for @summary do |f| %>
    <div class="form-group">
      <%= f.label :description %>
      <%= f.text_field :description, class: 'form-control', placeholder: "Enter summary here" %>
    </div>
    <div class="form-group">
      <%= f.submit "Save", class: 'btn btn-success' %>
    </div>
    <% end %>
  </div>
</div>